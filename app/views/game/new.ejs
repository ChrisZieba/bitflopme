<% include ../partials/header.ejs %>

<div class="jumbotron sub">
	<div class="container">
		<h1>Start a New Game</h1>
	</div>
	
</div>

<div class="container">
	

	<div class="row">


		<div class="col-md-4">  

			<form role="form" method="post" action="/game/new" class="well">

				
				<input type="hidden" name="_csrf" value="<%= token %>"/>

				<div class='form-group <% if (errors.tableName) { %>has-error<% } %>'>
					
					<label class="control-label">Name</label>
					<input class="form-control mg-top-bot-5" name="tableName" size="30" type="text" placeholder="Table Name" />
					<% if (errors.tableName) { %>
						<div class="alert alert-danger"><p><%= errors.tableName.msg %></p></div>
					<% } %>

				</div>

				<div class='form-group <% if (errors.tableTimer) { %>has-error<% } %>'>
					
					<label class="control-label">Timer</label>

					<select class="form-control mg-top-bot-5" name="tableTimer">
						<option>None</option>
					</select>

					<% if (errors.tableTimer) { %>
						<div class="alert alert-danger"><p><%= errors.tableTimer.msg %></p></div>
					<% } %>

				</div>

				<div class="form-group">

					<input type="submit" class="btn btn-lg btn-block btn-info" value="Create New Game" />

				</div>

			</form>
		</div>

		<div class="col-md-8">  
			<h3>Things to Keep in Mind</h3>
			<p>Bitflop.me is still early in it's development (alpha), so if you find any bugs during game play, please <a href="/contact">let us know</a>, or just feel free to drop us a line with any comments, suggestions, or feature requests!</p>

			<p>All table games do not have a timer, so that means you can take as long as you want to make your play. This is currently being worked on, and will be implemeneted in our next release. For more information on this, or other features to be added, please visit our <a href="/roadmap">roadmap</a>.</p>
		</div>
	</div>


</div>

<% include ../partials/footer.ejs %>