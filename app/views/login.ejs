<% include partials/header.ejs %>


<div class="jumbotron sub">
	<div class="container">
		<h1>Login</h1>
	</div>
	
</div>

<div class="container">
	


	<div class="row">
		<div class="col-sm-4">  
			<div class="row">
				<div class="col-sm-12">  
					<form role="form" method="post" action="/login" class="well">

						<input type="hidden" name="_csrf" value="<%= token %>"/>

						<div class="form-group <% if (errors.username) { %>has-error<% } %>">
							<label>Username</label>
							<input type="text" name="loginUsername" class="form-control mg-top-bot-5" placeholder="Username">
							<% if (errors.loginUsername) { %>
								<div class="alert alert-danger"><p><%= errors.loginUsername.msg %></p></div>
							<% } %>
						</div>

						<div class="form-group <% if (errors.loginPassword) { %>has-error<% } %>">
							<label>Password</label>
							<input type="password" name="loginPassword" class="form-control mg-top-bot-5" placeholder="Password">
							<% if (errors.loginPassword) { %>
								<div class="alert alert-danger"><p><%= errors.loginPassword.msg %></p></div>
							<% } %>
						</div>

						<div class="form-group">
							<div class="checkbox">
								<label>
									<input type="checkbox"> Remember me
								</label>
							</div>
						</div>

						<div class="form-group">
							<input type="submit" class="btn btn-block btn-primary mg-top-bot-5" value="Login" />
						</div>


					</form>
				</div>
			</div>
			<div class="row">
				<div class="col-sm-12">  

					<a href="/register" class="btn btn-default btn-block">Not a member yet? <strong>Register Here</strong></a>

				</div>
			</div>
		</div>

		<div class="col-sm-8">  
			<h3>A playing experience like no other!</h3>
			<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>

			<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>

			<div class="row">
				<div class="col-sm-6">  

					<h4>No email required</h4>

					<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>

					<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>
				</div>
				<div class="col-sm-6">  

					<h4>BitCoin</h4>

					<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>

					<p>If you're using sessions, you can just add that reg_error object to the req.session object before your redirect. Then it will be available on the req.session object when you're loading the previous page.</p>
				</div>
			</div>
		</div>
	</div>


</div>

<% include partials/footer.ejs %>